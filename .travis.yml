before_install :
  - sudo apt-get -qq update
  - sudo apt-get install -y libboost-all-dev

language : cpp

script:
  - cd Tests
  - mkdir build
  - cd build
  - cmake -G "Unix Makefiles" ..
  - make
  - ./tests

